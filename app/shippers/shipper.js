"use strict";
class Shipper {
    constructor() {
        this.getRates = (params) => { };
        this.createShipment = (params) => { };
        this.getArtifact = (uri) => { };
    }
}
exports.Shipper = Shipper;
